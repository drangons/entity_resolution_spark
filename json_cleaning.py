# -*- coding: utf-8 -*-
"""
Created on Fri Jul 31 12:55:36 2015

@author: dikshith

Python helper function 
"""
import json
from pyspark import SparkContext as sc


RDD.saveAsTextFile(Outputfile)


data = input.map(lambda x : json.loads(x))

(data.filter(lambda x : x['Pandas']).map(lambda x : json.dumps(x))
                                    saveasTextFile(outputFile))
                                    
import stringIO

def loadRecord(line):
    """Parse a csv line"""
    input 
            

